description: >-
  Other elements involved in an action. Only for Remove actions. The remove
  action on an Entity might involve other elements. i.e. object properties on
  removed classes
type: object
properties:
  nodes:
    type: array
    items:
      $ref: ./Node.yaml
  edges:
    type: array
    items:
      $ref: ./Edge.yaml
  Hierarchies:
    type: array
    items:
      $ref: ./Hierarchy.yaml
