get:
  description: Returns the ontology draft {ontologyName}
  parameters:
    - name: ontologyName
      in: path
      required: true
      type: string
  consumes:
    - application/json
  produces:
    - application/json
  tags:
    - ONTOLOGY DRAFTS
  responses:
    '200':
      description: OK
      schema:
        $ref: '#/definitions/com.mwsx.model.rdfgraph.RDFGraph'
    '401':
      description: The user does not have sufficient privileges to invoke this service
    '403':
      description: User not authorized due to wrong credentials
    '500':
      description: An internal server error occurred while performing the request
    '510':
      description: This method is currently not implemented
put:
  description: >-
    Modifies the ontology draft {ontologyName} (called when using the ontology
    builder to save the draft)
  parameters:
    - name: ontologyName
      in: path
      required: true
      type: string
    - name: arg3
      in: body
      required: false
      schema:
        $ref: '#/definitions/com.mwsx.model.rdfgraph.RDFGraph'
  consumes:
    - application/json
  produces:
    - application/json
  tags:
    - ONTOLOGY DRAFTS
  responses:
    '200':
      description: OK
      schema:
        type: array
        items:
          $ref: '#/definitions/com.mwsx.model.rdfgraph.RDFGraph'
    '401':
      description: The user does not have sufficient privileges to invoke this service
    '403':
      description: User not authorized due to wrong credentials
    '500':
      description: An internal server error occurred while performing the request
    '510':
      description: This method is currently not implemented
post:
  description: Adds a new draft to the list of all ontology drafts
  parameters:
    - name: ontologyName
      in: path
      required: true
      type: string
    - name: arg3
      in: body
      required: false
      schema:
        $ref: '#/definitions/com.mwsx.model.rdfgraph.RDFGraph'
  consumes:
    - application/json
  produces:
    - application/json
  tags:
    - ONTOLOGY DRAFTS
  responses:
    '200':
      description: OK
      schema:
        type: array
        items:
          $ref: '#/definitions/com.mwsx.model.rdfgraph.RDFGraph'
    '401':
      description: The user does not have sufficient privileges to invoke this service
    '403':
      description: User not authorized due to wrong credentials
    '500':
      description: An internal server error occurred while performing the request
    '510':
      description: This method is currently not implemented
delete:
  description: Deletes the ontology draft {ontologyName}
  parameters:
    - name: ontologyName
      in: path
      required: true
      type: string
  consumes:
    - application/json
  produces:
    - application/json
  tags:
    - ONTOLOGY DRAFTS
  responses:
    '200':
      description: OK
      schema:
        $ref: '#/definitions/com.mwsx.model.rdfgraph.RDFGraph'
    '401':
      description: The user does not have sufficient privileges to invoke this service
    '403':
      description: User not authorized due to wrong credentials
    '500':
      description: An internal server error occurred while performing the request
    '510':
      description: This method is currently not implemented
