post:
  tags: [Persistence]
  summary: Tentatively fixes mapping deprecations
  operationId: postOwlOntologyNameVersionMappingIDFixDeprecated
  parameters:
  - name: name
    in: path
    required: true
    schema: {type: string}
  - name: mapID
    in: path
    required: true
    schema: {type: string}
  - name: version
    in: query
    required: true
    schema: {type: string}
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema: {type: integer}
    '401': {description: Unhauthorized}
