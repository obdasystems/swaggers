get:
  tags: [Visualize]
  summary: GET mwsx/owlOntology/{name}/version/gscape/check
  operationId: getOwlOntologyNameVersionGscapeCheck
  parameters:
  - name: name
    in: path
    required: true
    schema: {type: string}
  - name: version
    in: query
    required: true
    schema: {type: string}
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema: {type: boolean}
    '401': {description: Unhauthorized}
