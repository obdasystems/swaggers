get:
  tags: [Drivers]
  summary: Returns the list of available JDBC driver class names
  operationId: getDatasourceDriver
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: array
            items: {type: string}
    '401': {description: Unhauthorized}
