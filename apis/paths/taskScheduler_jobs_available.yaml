get:
  tags: [Persistence]
  summary: Lists the jobs available in the scheduler folder
  operationId: getTaskSchedulerJobsAvailable
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: array
            items: {$ref: ../components/schemas/MwsxTaskSchedulerJobEntry.yaml}
    '401': {description: Unhauthorized}
