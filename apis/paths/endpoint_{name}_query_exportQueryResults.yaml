post:
  tags: [ExportedQuery]
  summary: Exports on a separate thread the results of a query running on the given
    endpoint, the export thread identifier is returned
  operationId: postOwlOntologyNameVersionQueryIDExportResults
  parameters:
  - name: name
    in: path
    required: true
    schema: {type: string}
  requestBody:
    content:
      application/json:
        schema: {$ref: ../components/schemas/MWSXExportInfo.yaml}
    required: true
  responses:
    '200':
      description: OK
      content:
        text/plain:
          schema: {type: string}
    '401': {description: Unhauthorized}
