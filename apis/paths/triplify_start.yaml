post:
  tags: [Execution]
  summary: Starts a new thread of mappings triplification with the given parameters
  operationId: postMappingsTriplificationStart
  requestBody:
    content:
      application/json:
        schema: {$ref: ../components/schemas/MWSXOBDATriplificationRequest.yaml}
    required: true
  responses:
    '200':
      description: OK
      content:
        text/plain:
          schema: {type: string}
    '401': {description: Unhauthorized}
