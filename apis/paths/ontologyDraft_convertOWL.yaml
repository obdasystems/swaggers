post:
  tags: [Persistence]
  summary: Convert an OWL file in RDFGraph model
  operationId: postOntologyDraftConvertOWL
  requestBody:
    content:
      application/json:
        schema: {$ref: ../components/schemas/FileInfo.yaml}
    required: true
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema: {$ref: ../components/schemas/RDFGraph.yaml}
    '401': {description: Unhauthorized}
