put:
  tags: [Upload]
  summary: PUT mwsx/knowledgeGraph/upload/import/csv
  operationId: putKnowledgeGraphUploadFileCsvImport
  parameters:
  - name: synchronousMode
    in: query
    required: true
    schema: {type: boolean}
  requestBody:
    content:
      application/json:
        schema: {$ref: ../components/schemas/CsvImportInfo.yaml}
    required: true
  responses:
    '200':
      description: OK
      content:
        text/plain:
          schema: {type: string}
    '401': {description: Unhauthorized}
