get:
  tags: [Logical]
  summary: Returns the list of ignored ontology import IRIs
  operationId: getOwlOntologyNameVersionIgnoredImports
  parameters:
    - name: name
      in: path
      required: true
      schema: { type: string }
    - name: version
      in: query
      required: true
      schema: { type: string }
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: array
            items: { type: string }
    "401": { description: Unhauthorized }
