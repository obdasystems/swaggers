get:
  tags: [Highlights]
  summary: Get the state of the thread computing a shortest path on the ontology
  operationId: get_shortest_path_thread_state
  parameters:
    - name: name
      in: path
      required: true
      schema: { type: string }
    - name: version
      in: query
      required: true
      schema: { type: string }
    - name: threadId
      in: query
      required: true
      schema: { type: string }
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: "../components/schemas/HighlightsPathsThreadState.yaml"
    "401": { description: Unhauthorized }
