get:
  tags: [Server]
  summary: Returns the license expiration date
  operationId: getServerLicenseExpirationDate
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema: {$ref: ../components/schemas/LicenseInfo.yaml}
    '401': {description: Unhauthorized}
