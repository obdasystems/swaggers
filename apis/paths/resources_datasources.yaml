get:
  tags: [Resources]
  summary: GET mwsx/resources/datasources
  operationId: getResourcesDsIds
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: array
            items: {type: string}
    '401': {description: Unhauthorized}
