get:
  tags: [Persistence]
  summary: Returns all endpoints with status running
  operationId: getRunningEndpoints
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema: {$ref: ../components/schemas/Endpoints.yaml}
    '401': {description: Unhauthorized}
