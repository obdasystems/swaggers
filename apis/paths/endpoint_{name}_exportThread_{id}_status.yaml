get:
  tags: [ExportedQuery]
  summary: Returns the status of an export thread running on the given endpoint
  operationId: getOwlOntologyNameVersionQueryIDExportThreadIdStatus
  parameters:
  - name: name
    in: path
    required: true
    schema: {type: string}
  - name: id
    in: path
    required: true
    schema: {type: string}
  responses:
    '200':
      description: OK
      content:
        text/plain:
          schema: {type: string}
    '401': {description: Unhauthorized}
