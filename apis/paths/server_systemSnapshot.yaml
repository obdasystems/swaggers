get:
  tags: [Server]
  summary: Computes and returns the system storage report
  operationId: getSystemSnapshot
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema: {$ref: ../components/schemas/Snapshot.yaml}
    '401': {description: Unhauthorized}
