get:
  tags: [Geocoding]
  summary: Returns the list of available Geocoding servers
  operationId: getGeocodingServers
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema: {$ref: ../components/schemas/GeocodingServers.yaml}
    '401': {description: Unhauthorized}
