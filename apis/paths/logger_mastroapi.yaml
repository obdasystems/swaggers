get:
  tags: [Logger]
  summary: GET mwsx/logger/mastroapi
  operationId: getSetMastroapiLogger
  parameters:
  - name: mode
    in: query
    required: true
    schema: {type: string}
  responses:
    '200':
      description: OK
      content:
        '*/*':
          schema: {type: integer, format: int32}
    '401': {description: Unhauthorized}
