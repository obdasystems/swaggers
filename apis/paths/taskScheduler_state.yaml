get:
  tags: [State]
  summary: Starts the task scheduler
  operationId: getTaskSchedulerState
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema: {type: boolean}
    '401': {description: Unhauthorized}
put:
  tags: [State]
  summary: Starts the task scheduler
  operationId: putTaskSchedulerState
  parameters:
  - name: action
    in: query
    required: true
    schema: {type: string}
  responses:
    '200': {description: OK}
    '401': {description: Unhauthorized}
