title: EntitiesExamplesInfo
type: object
required: [values, startTime, endTime, state, percentage]
properties:
  values:
    type: object
    additionalProperties: { $ref: ./EntityExamples.yaml }
  startTime: { type: integer, format: int64, default: 0 }
  endTime: { type: integer, format: int64, default: 0 }
  state: { $ref: ./IMwsxThreadState.yaml }
  percentage: { type: number, default: 0 }
