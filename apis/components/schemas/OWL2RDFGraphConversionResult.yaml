type: object
required:
  - rdfGraph
  - rejectedAxioms
  - acceptedAxioms
  - originalAxioms 
properties:
  rdfGraph: { $ref: ./RDFGraph.yaml }
  rejectedAxioms: { type: number, default: 0 }
  acceptedAxioms: { type: number, default: 0 }
  originalAxioms: { type: number, default: 0 }