type: object
required:
  - rdfGraph
  - rejectedAxioms
  - acceptedAxioms
  - originalAxioms 
properties:
  rdfGraph: { $ref: ./RDFGraph.yaml }
  rejectedAxioms: { type: integer, default: 0, format: int32 }
  acceptedAxioms: { type: integer, default: 0, format: int32 }
  originalAxioms: { type: integer, default: 0, format: int32 }