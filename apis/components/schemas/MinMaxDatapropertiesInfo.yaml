title: MinMaxDatapropertiesInfo
type: object
required: [mins, maxs, startTime, endTime, state, percentage]
properties:
  mins:
    type: object
    additionalProperties: { $ref: ./EntityMinMax.yaml }
  maxs:
    type: object
    additionalProperties: { $ref: ./EntityMinMax.yaml }
  startTime: { type: integer, format: int64, default: 0 }
  endTime: { type: integer, format: int64, default: 0 }
  state: { $ref: ./IMwsxThreadState.yaml }
  percentage: { type: number, default: 0 }
