title: InstanceCheckingInfo
type: object
properties:
  resultClasses:
    type: array
    items: { $ref: ./OntologyEntity.yaml }
  percentage: { type: number, default: 0 }
  startTime: { type: integer, format: int64, default: 0 }
  endTime: { type: integer, format: int64, default: 0 }
  state:
    type: string
    enum: [READY, RUNNING, FINISHED, ERROR, STOPPED]
