type: object
required: [id, name]
properties:
  id: {type: integer, default: 0}
  name: {type: string}
  nodes:
    type: array
    items: {$ref: ./Node.yaml}
  edges:
    type: array
    items: {$ref: ./Edge.yaml}
  lastViewportState: {$ref: ./Viewport.yaml}
