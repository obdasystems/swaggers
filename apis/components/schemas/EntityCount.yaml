title: EntityCount
type: object
required: [entity, count, state]
properties:
  count: { type: integer, format: int64, default: 0 }
  state: { $ref: ./IMwsxThreadState.yaml }
  error: { type: string }
  entity: { $ref: ./OntologyEntity.yaml }
