title: EntityCount
type: object
required:
  - entity
  - count
  - state
properties:
  count:
    type: integer
    format: int64
  state:
    type: string
  error:
    type: string
  entity:
    $ref: ./OntologyEntity.yaml
