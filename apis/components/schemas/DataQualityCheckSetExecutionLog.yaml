title: DataQualityCheckSetExecutionLog
type: object
properties:
  ontologyConstraints:
    type: array
    items: {$ref: ./DataQualityCheckExecutionLog.yaml}
  endpoint: {type: string}
  endpointInfo: {$ref: ./Endpoint.yaml}
  endTimestamp: {type: integer, format: int64, default: 0}
  user: {type: string}
  checkSetId: {type: string}
  timestamp: {type: integer, format: int64, default: 0}
