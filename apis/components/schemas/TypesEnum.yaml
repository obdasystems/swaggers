description: >-
  Contiene tutti i tipi di nodi/archi orginirari dal Graphol per evitare di
  duplicare gli enumeratori. Nella rappresentazione Floaty/vkg vengono usati
  questi valori.

  NODI class data-property class-instance (vkg) individual (floaty) union
  disjoint-union iri (floaty iri range di annotazioni che non sono entità)

  ARCHI object-property annotation-property instance-of input inclusion
  equivalence attribute-edge union disjoint-union complete-union
  complete-disjoint-union
type: string
enum: [class, object-property, data-property, annotation-property, individual, class-instance, domain-restriction, range-restriction, union, complete-union, disjoint-union, complete-disjoint-union, complement, intersection, enumeration, has-key, role-inverse, role-chain, datatype-restriction, value-domain, property-assertion, literal, facet, neutral, value, inclusion, equivalence, instance-of, input, same, different, membership, attribute-edge, iri]
x-enum-varnames:
  - CLASS
  - OBJECT_PROPERTY
  - DATA_PROPERTY
  - ANNOTATION_PROPERTY
  - INDIVIDUAL
  - CLASS_INSTANCE
  - DOMAIN_RESTRICTION
  - RANGE_RESTRICTION
  - UNION
  - COMPLETE_UNION
  - DISJOINT_UNION
  - COMPLETE_DISJOINT_UNION
  - COMPLEMENT
  - INTERSECTION
  - ENUMERATION
  - HAS_KEY
  - ROLE_INVERSE
  - ROLE_CHAIN
  - DATATYPE_RESTRICTION
  - VALUE_DOMAIN
  - PROPERTY_ASSERTION
  - LITERAL
  - FACET
  - NEUTRAL
  - VALUE
  - INCLUSION
  - EQUIVALENCE
  - INSTANCE_OF
  - INPUT
  - SAME
  - DIFFERENT
  - MEMBERSHIP
  - ATTRIBUTE_EDGE
  - IRI

