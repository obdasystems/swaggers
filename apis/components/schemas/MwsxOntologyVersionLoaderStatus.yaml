title: MwsxOntologyVersionLoaderStatus
type: object
properties:
  id: { type: string }
  state: { $ref: ./IMwsxThreadState.yaml }
  timeStart: { type: integer, format: int64, default: 0 }
  timeElapsed: { type: integer, format: int64, default: 0 }
  currentStep: { type: string }
  progress: { type: number, format: double, default: 0 }
