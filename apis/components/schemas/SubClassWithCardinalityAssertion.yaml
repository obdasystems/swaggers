title: SubClassWithCardinalityAssertion
type: object
allOf:
- {$ref: AbstractDataQualityConstraint.yaml}
properties:
  cardinalityType:
    type: string
    enum: [EXACT, MAX, MIN]
  property: {$ref: ./OntologyEntity.yaml}
  direct: {type: boolean, default: false}
  filler: {$ref: ./OntologyEntity.yaml}
  cardinality: {type: integer, format: int32}
